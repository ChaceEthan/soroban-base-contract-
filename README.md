# Soroban Base Contract

This repository contains a basic Soroban smart contract written in Rust for the Stellar network.

## Structure

- `contracts/soroban_contract/` - The main smart contract.
- `src/lib.rs` - Contract implementation.
- `src/test.rs` - Unit tests for the contract.
